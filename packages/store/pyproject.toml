[project]
name = "store"
version = "0.1.0"
description = "Vector store layer — Qdrant"
requires-python = ">=3.12"
dependencies = [
    "schema",
    "qdrant-client>=1.14",
]

[tool.setuptools.packages.find]
where = ["src"]
