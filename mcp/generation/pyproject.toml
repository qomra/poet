[project]
name = "mcp-generation"
version = "0.1.0"
description = "MCP server: generation"
requires-python = ">=3.12"
dependencies = [
    "schema",
    "db",
    "store",
    "mcp>=1.6",
]

[project.scripts]
mcp-generation = "mcp.generation.server:main"
